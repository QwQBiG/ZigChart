# 贡献指南

[English](CONTRIBUTING.md) | [简体中文](CONTRIBUTING.zh-CN.md)

修改接口前，请阅读 [AGENTS.md](AGENTS.md)、[架构](docs/architecture.zh-CN.md)和[数据约定](docs/data-contract.zh-CN.md)。ZigChart 使用 [Apache License 2.0](LICENSE)，贡献内容必须与该许可兼容。

## 开发流程

1. 使用 [README.md](README.zh-CN.md) 中规定的版本和安装命令。检查当前工作区，保留无关修改。
2. 让每项改动聚焦具体行为。数据语义、Wasm ABI 或内存归属变化时同步更新约定；文档描述的内容变化时，同时更新英文和简体中文版本。
3. 为有意义的不变量或回归场景添加测试。优先使用可复现的固定数据，并明确预期值。
4. 运行 `npm run check:architecture`、`npm test` 和 `npm run build`。文档修改后运行 `npm run check:docs`。UI 修改后按[浏览器验证清单](docs/verification.zh-CN.md)检查交互及浏览器错误。
5. 请求审查前检查完整净差异，包括新增文件。说明实际执行的检查、结果及尚未验证的内容。

源码、标识符和代码注释使用英文。议题可以使用英文或简体中文；提交和拉取请求描述必须同时包含两种语言。文档同时维护英文和简体中文：英文文件保留原名，中文对应版本使用 `.zh-CN.md` 后缀。每组文档应提供语言链接，并保留一致的事实、示例、来源链接和限制说明。内部文档链接指向对应语言版本。共用的 `AGENTS.md` 和拉取请求模板在同一文件中提供双语；上游法律声明原样保留。所有文本文件使用 UTF-8 和 LF，包括脚本；`.editorconfig` 和 `.gitattributes` 定义 Windows 与 Unix 检出时的规则。注释应说明当前行为和维护约束。

`check:docs` 检查文档配对、双向语言链接和本地链接。如果将 `DOCS_BASE_REF` 设置为用于比较的提交，还会检查发生变化的文档是否同时更新了配对文件；CI 会在可用时提供此引用。这些结构检查不能证明语义一致，请在请求审查前核对翻译是否准确对应。

## 边界

- 从 `web/src/data/contracts.ts` 导入行情提供方类型，数据依赖保留在 `data/` 内。可复用模块不得导入 `app/`，`main.ts` 是启动入口例外。绘图区交互归 `app/chart-input.ts` 管理，并通过 `npm run check:architecture` 拒绝相对运行时依赖循环。
- 不在 `core/` 中使用浏览器 API、网络或 Canvas 调用。
- 数值数据通过批次交换；ABI 约定变化时更新其版本。
- 不跨调用保留借用的 Wasm 内存视图。TypeScript 桥接返回复制后的输出。
- 修改状态前校验整个批次；保持排序、更新语义和视口锚定。
- 保留指标预热阶段的空缺，并测试替换、历史前插及追加行为。
- 在宿主边界完成行情提供方数据的规范化。采购数据、凭据和私有配置不得纳入版本控制。嵌入浏览器包的凭据无法保密。
- 未来脚本和策略执行应通过宿主边界扩展，不将这些系统加入图表 MVP。

## 依赖与审查

固定直接依赖版本，有意识地更新锁文件，并检查实际发布的产物。采用代码或素材前，按[第三方说明](THIRD_PARTY.zh-CN.md)记录来源并核实许可证和声明。许可证变更必须经过所有者审查和批准。

拉取请求应说明具体问题、修改后的行为、相关验证和重要限制。不要在仓库中堆积重复的差异报告。性能结论必须有可复现的测量，并说明环境及负载；没有测量时，只描述预期优化，不宣称取得了实测改进。
